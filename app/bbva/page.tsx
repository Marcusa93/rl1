// App del participante — Laboratorio de IA · BBVA (clase inicial).
// Un solo QR para toda la hora: se elige el área y el celular sigue solo
// las actividades que Marco abre desde la presentación.

import { AlumnoBbva } from "@/components/bbva/alumno";

export default function BbvaPage() {
  return <AlumnoBbva />;
}
