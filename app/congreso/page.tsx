// App del público — Congreso · "Vibe coding para abogados".
// Un solo QR para toda la charla: sin registro, sin nombre; el celular sigue
// solo las interacciones que Marco va abriendo desde la pantalla grande.

import { PublicoCongreso } from "@/components/congreso/publico";

export default function CongresoPage() {
  return <PublicoCongreso />;
}
